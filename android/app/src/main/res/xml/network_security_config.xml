<?xml version="1.0" encoding="utf-8"?>
<!--
  The backend runs over plain HTTP in development (http://localhost:5000).
  Android 9+ blocks cleartext by default, so without this every request fails
  with a bare connection error that looks like the server is down.

  The exemption is scoped to loopback and private-range hosts rather than
  enabled globally: a production build talking to a real domain still gets
  full TLS enforcement, so this cannot silently weaken the shipped app.
  10.0.2.2 is the Android emulator's alias for the host machine.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
    </domain-config>
</network-security-config>
